# ⚠️ WARNING: These are development-only values!
# ⚠️ NEVER use these values in production!
# ⚠️ Change all passwords and secrets before deploying!

# PostgreSQL Configuration
POSTGRES_USER=chi_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=chi_db

# Database Connection URL (uses custom port 5434)
DATABASE_URL=postgresql://chi_user:change_me_in_production@localhost:5434/chi_db

# Environment
NODE_ENV=development
